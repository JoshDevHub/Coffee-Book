<% flash.each do |type, message| %>
  <div class="<%= flash_class(type) %> py-2 px-1 text-black" data-controller="flash">
    <%= message %>
    <button type="button" data-action="flash#dismiss">X</button>
  </div>
<% end %>
