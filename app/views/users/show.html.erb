<!-- <div class="grid grid-rows-2 grid-cols2 grid-flow-col"> -->
<div class="flex flex-col md:flex-row gap-8 items-center md:items-start">
  <div class="md:shadow-lg px-10 pt-4 border border-gray-300 flex flex-col
              items-center rounded-md md:sticky md:top-24 w-1/2 md:w-1/3 h-[300px]
              mt-12 min-w-[320px] max-w-[500px] mr-8 ml-12">
    <%= render @profile %>
  </div>

  <div class="flex flex-col gap-4 w-1/2 min-w-[320px] max-w-[500px]">
    <h1 class="mx-auto text-2xl font-bold my-0 text-slate-800">
      <%= @user.first_name %>'s Posts:
    </h1>
    <%= render @user.posts %>
  </div>
</div>
